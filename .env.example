# Local database connection (adjust as needed)
PGUSER=postgres
PGPASSWORD=is0Merate
# JWT for local dev ─ change the value to anything random if you like
JWT_SECRET=test-jwt-secret
JWT_EXPIRES_IN=1h